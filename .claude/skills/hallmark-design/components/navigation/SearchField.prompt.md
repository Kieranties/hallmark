Search input — the only text input in the docs surface. Focus turns the border gold; there is no focus glow.

```jsx
<SearchField icon={<i data-lucide="search" style={{width:14,height:14}} />} />
```
