Top bar — translucent, blurred, hairline underline. The active section is marked with a 2px gold rule, never a filled pill.

```jsx
<Navbar items={[{id:"practice",label:"Practice"}]} active="practice" right={<ThemeToggle />} />
```
