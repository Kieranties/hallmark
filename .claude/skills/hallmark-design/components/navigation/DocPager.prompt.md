Closes every docs page.

```jsx
<DocPager previous={{ label: 'The door' }} next={{ label: 'States and criteria' }} />
```

Omit a side and its space is held, so the remaining card keeps its half. Direction label is mono uppercase; page title is Spectral.
