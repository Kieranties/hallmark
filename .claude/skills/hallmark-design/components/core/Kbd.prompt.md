A keyboard key, for shortcuts in docs prose and the search affordance.

```jsx
Press <Kbd>Ctrl</Kbd> <Kbd>K</Kbd> to search.
```

One key per element; write the separator as plain text. Never combine keys inside one `Kbd`.
