Admonition — the tinted plate carries the meaning; there is no coloured left border.

```jsx
<Callout kind="warning" title="Nothing prunes the stack">
  Layers only ever add, so the merged set only ever grows.
</Callout>
```

`proof` and `failure` are Hallmark-specific: use them for evidence that held and evidence that did not.
