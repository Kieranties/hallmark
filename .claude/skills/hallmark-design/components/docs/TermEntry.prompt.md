Glossary entry — the atom of the terminology page.

```jsx
<TermEntry id="actor" term="Actor" kind="Party"
  definition={<>A human or an agent — the thing that actually holds a role.</>}
  seeAlso={[{ label: "Role", href: "#role" }]} />
```

Cross-references use the link treatment (ink + hairline underline, gold on hover), not the gold dotted term mark — the mark means "defined here", the link means "go there". Entries are separated by a hairline top border, never by cards. Definitions open with a bolded lead clause in prose.
