The system's signature move: calling out defined vocabulary inside otherwise quiet prose.

```jsx
Every <Mark href="/glossary#actor">actor</Mark> must be distinctly identifiable.
A state of <Mark variant="identifier">Accepted</Mark> means the sift passed.
```

Mark a term on its **first** appearance in a page, not on every occurrence. Never use `keyword` and `term` in the same sentence.
