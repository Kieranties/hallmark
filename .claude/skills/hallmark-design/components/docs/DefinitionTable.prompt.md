Reference table — hairline rows, uppercase micro headers, no zebra striping and no outer border.

```jsx
<DefinitionTable columns={["Role", "Does", "Context"]} rows={[["Worker", "Advances the work", "Accumulates"]]} />
```
